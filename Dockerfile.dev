FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm install

# Copy application files
COPY . .

# Create tmp directory for audio conversion
RUN mkdir -p .tmp

EXPOSE 3000

CMD ["npm", "run", "dev"]
